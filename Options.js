class options{
    constructor(){
        this.button1=createButton("I'm great")
        this.button2=createButton("I'm fine")
        this.button3=createButton("I could be better, but it's okay")
        this.button4=createButton("Compared to last year, I'm fine")
        this.answer1=createButton("I already have")
        this.answer2=createButton("Yes I will!")
        this.answer3=createButton("I'll not get EVERYTHING I wish for but I will get something")
        this.answer4=createButton("No, Expectations always fail you")
        this.ans1=createButton("Everyone is great! They all have some good in them!")
        this.ans2=createButton("All have a good side but not many stick to it")
        this.ans3=createButton("You can't trust anyone these days. They will all break their promises. It is better to be independent ")
        this.click1=createButton("Hopefully, it will be wonderful. ")
        this.click2=createButton("I will stutter a bit, and forget some lines but overall it'll be okay ")
        this.click3=createButton("I'll not give the speech because I can't do it")
        this.sol1=createButton("Half Empty")
        this.sol2=createButton("Half full")
        this.sol3=createButton("half empty and half full")
        this.solution1=createButton("It was great! ")
        this.solution2=createButton("It could be better")
        this.solution3=createButton("Please tell me the results quickly")
      
        
    }
    display(){
        this.button1.position(10,290)
        this.button2.position(10,320)
        this.button3.position(10,350)
        this.button4.position(10,380)
        this.answer1.position(10,490)
        this.answer2.position(10,520)
        this.answer3.position(10,550)
        this.answer4.position(10,580);
        this.ans1.position(10,690);
        this.ans2.position(10,720)
        this.ans3.position(10,750);
        this.click1.position(10,890);
         this.click2.position(10,920)
        this.click3.position(10,950);
        this.sol1.position(10,1300);
        this.sol2.position(10,1330)
        this.sol3.position(10,1360)
        this.solution1.position(10,1470)
        this.solution2.position(10,1500)
        this.solution3.position(10,1530)
       
    }
}